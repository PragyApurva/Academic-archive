declare module 'react-live-search' {
    import * as React from 'react';

    interface LiveSearchProps {
      // Define the props for the LiveSearch component
      // Example:
      // items: Array<any>;
      // onChange: (value: string) => void;
    }

    export class LiveSearch extends React.Component<LiveSearchProps> {}
  }
